name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Verify optional environment setup
        run: |
          test ! -f .env.local
          npm run setup:env
          test -f .env.local
          rm .env.local
      - run: npm run lint
      - run: npm test
      - run: npm run build
      - name: Verify Netlify deployment files
        run: |
          test -f netlify.toml
          test -f netlify/functions/api.ts
          test -f netlify/functions/osint.ts
          test -f netlify/functions/client-config.ts
          test -f lib/api.ts
          test -f lib/osintProviders.ts
          test -f lib/aisStreamHub.ts
      - name: Smoke test without an environment file
        run: |
          test ! -f .env.local
          npm run dev > /tmp/aetherglobe.log 2>&1 &
          SERVER_PID=$!
          trap 'kill "$SERVER_PID" 2>/dev/null || true' EXIT

          for attempt in {1..30}; do
            if curl -fsS http://127.0.0.1:3000/api/health > /tmp/health.json; then
              break
            fi
            sleep 1
          done

          curl --max-time 35 -fsS \
            -X POST http://127.0.0.1:3000/api/intelligence \
            -H 'Content-Type: application/json' \
            -d '{"lat":19.9975,"lng":73.7898}' \
            > /tmp/intelligence.json

          node --input-type=module -e "
            import fs from 'node:fs';
            const health = JSON.parse(fs.readFileSync('/tmp/health.json', 'utf8'));
            const intelligence = JSON.parse(fs.readFileSync('/tmp/intelligence.json', 'utf8'));
            if (health.intelligenceMode !== 'direct-source-brief') throw new Error('Unexpected source brief mode');
            if (health.localReceiverConfigured !== false) throw new Error('Local receiver should be optional');
            if (health.aviationstackConfigured !== false) throw new Error('Aviationstack should be optional');
            if (health.aisstreamConfigured !== false) throw new Error('AISstream should be optional');
            if (intelligence.mode !== 'direct-source-brief') throw new Error('Direct source brief was not used');
            if (intelligence.model !== null) throw new Error('A language model should not be configured');
            if (typeof intelligence.report !== 'string' || intelligence.report.length < 100) throw new Error('Source brief missing');
          "
