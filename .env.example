# Server-only secret used by /api/intelligence. Never expose this with a VITE_ prefix.
GEMINI_API_KEY=

# Optional provider keys. Endpoints return an explicit unavailable response when omitted.
CHECKWX_API_KEY=
NASA_API_KEY=

# Runtime configuration
PORT=3000
NODE_ENV=development
